# Phase Plans

Phase plans define outcomes, dependency boundaries, and exit evidence. They intentionally do not repeat endpoint fields, domain schemas, or detailed task instructions; those belong to contracts and ready task files.

Read `../master-roadmap.md`, then the current phase, then the selected task in `../../16-agent-tasks/`.
